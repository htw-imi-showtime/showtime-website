name: deploy staging (https://htw-imi-showtime.github.io/staging)

on:
  push:
    branches: [ main ]

  workflow_dispatch:

jobs:
  deploy:
    if: github.repository == 'htw-imi-showtime/showtime-website'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        submodules: true

    - name: Hugo setup
      uses: peaceiris/actions-hugo@v2
      with:
        hugo-version: '0.114.1'
        extended: true

    - name: Build
      run: |
        ./bin/hugo-temp.sh
        hugo \
          --source hugo-temp \
          --environment staging \
          --minify \
          --baseURL "https://htw-imi-showtime.github.io/staging/" \
          --buildDrafts 
        mv hugo-temp/public public

    - name: Deploy
      uses: peaceiris/actions-gh-pages@v3
      with:
        deploy_key: ${{ secrets.STAGING_DEPLOY_KEY }}
        external_repository: htw-imi-showtime/staging
        publish_branch: main
